---
phase: 06-content-migration
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/pages/blog/index.astro
  - src/layouts/BlogPost.astro
autonomous: true
requirements:
  - CONT-03
  - CONT-04
  - CONT-05

must_haves:
  truths:
    - "The /blog page lists all posts with Card components, language badges, dates, descriptions, and ghost CTA buttons"
    - "Individual blog post pages render all markdown elements with correct prose typography"
    - "The RSS feed at /rss.xml includes all blog posts with title, description, pubDate, and link"
  artifacts:
    - path: "src/pages/blog/index.astro"
      provides: "Blog listing page with new design system"
      contains: "Card"
    - path: "src/layouts/BlogPost.astro"
      provides: "Blog post layout with prose styling"
      contains: "prose"
  key_links:
    - from: "src/pages/blog/index.astro"
      to: "src/components/ui/Card.astro"
      via: "import and render"
      pattern: "import Card"
    - from: "src/pages/blog/index.astro"
      to: "src/components/ui/Badge.astro"
      via: "import and render"
      pattern: "import Badge"
    - from: "src/pages/blog/index.astro"
      to: "src/components/ui/Button.astro"
      via: "import and render"
      pattern: "import Button"
    - from: "src/pages/rss.xml.js"
      to: "astro:content blog collection"
      via: "getCollection and spread"
      pattern: "getCollection.*blog"
---

<objective>
Restyle the blog listing page using the new design system (Card, Badge, Button) and verify prose typography on blog post pages and RSS feed functionality.

Purpose: The blog listing is currently unstyled divs -- it needs to match the BlogPreviewSection pattern from the landing page. Blog post prose styling and RSS feed need verification after schema changes.
Output: Styled blog listing page, verified prose rendering, confirmed RSS feed.
</objective>

<execution_context>
@/home/ng4e/gambetech/internal/repository/ng4e.github.io/.claude/get-shit-done/workflows/execute-plan.md
@/home/ng4e/gambetech/internal/repository/ng4e.github.io/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-content-migration/06-CONTEXT.md
@.planning/phases/06-content-migration/06-RESEARCH.md
@.planning/phases/06-content-migration/06-01-SUMMARY.md

@src/pages/blog/index.astro
@src/layouts/BlogPost.astro
@src/pages/rss.xml.js
@src/pages/blog/[...slug].astro
@src/components/sections/BlogPreviewSection.astro

<interfaces>
<!-- Card.astro props (src/components/ui/Card.astro) -->
```typescript
interface Props {
  variant?: "default" | "on-dark";
  class?: string;
}
// Renders: <div class="bg-white rounded-xl p-6 transition-all ..."><slot /></div>
```

<!-- Badge.astro props (src/components/ui/Badge.astro) -->
```typescript
interface Props {
  variant?: "accent" | "surface";
  class?: string;
}
```

<!-- Button.astro props (src/components/ui/Button.astro) -->
```typescript
interface Props {
  variant?: "primary" | "ghost";
  size?: "default" | "small";
  href?: string;
  class?: string;
}
```

<!-- FormattedDate.astro props -->
```typescript
interface Props {
  date: Date;
}
```

<!-- BlogPreviewSection card pattern (exact target to replicate): -->
```astro
<Card>
  <div class="flex items-center gap-3 mb-3">
    <Badge variant="surface" class="text-xs px-2 py-1">{post.data.lang === "fr" ? "FR" : "EN"}</Badge>
    <span class="text-sm text-text-secondary"><FormattedDate date={post.data.pubDate} /></span>
  </div>
  <h3 class="text-xl font-semibold text-primary mb-2">
    <a href={"/blog/" + post.id} class="hover:text-accent transition-colors">{post.data.title}</a>
  </h3>
  <p class="text-base text-text-secondary line-clamp-3 mb-4">{post.data.description}</p>
  <Button variant="ghost" size="small" href={"/blog/" + post.id}>Lire la suite</Button>
</Card>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle blog listing page with design system components</name>
  <files>src/pages/blog/index.astro</files>
  <read_first>src/pages/blog/index.astro, src/components/sections/BlogPreviewSection.astro, src/components/ui/Card.astro, src/components/ui/Badge.astro, src/components/ui/Button.astro</read_first>
  <action>
    **Rewrite `src/pages/blog/index.astro` to use the design system (per D-05, D-06, D-07, D-08):**

    Replace the current unstyled div-based listing with the exact card pattern from BlogPreviewSection.astro.

    The full target implementation:
    ```astro
    ---
    import BaseLayout from "../../layouts/BaseLayout.astro";
    import { getCollection } from "astro:content";
    import Card from "../../components/ui/Card.astro";
    import Badge from "../../components/ui/Badge.astro";
    import Button from "../../components/ui/Button.astro";
    import FormattedDate from "../../components/FormattedDate.astro";

    const posts = (await getCollection("blog")).sort(
      (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
    );
    ---

    <BaseLayout
      title="Idees et Opinions"
      description="...et autres questions existentielles sur la technologie, la science et la vie de tous les jours"
      headerTitle="Idées et Opinions"
    >
      <section class="py-20">
        <div class="max-w-7xl mx-auto px-4">
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            {posts.map((post) => (
              <Card>
                <div class="flex items-center gap-3 mb-3">
                  <Badge variant="surface" class="text-xs px-2 py-1">
                    {post.data.lang === "fr" ? "FR" : "EN"}
                  </Badge>
                  <span class="text-sm text-text-secondary">
                    <FormattedDate date={post.data.pubDate} />
                  </span>
                </div>
                <h3 class="text-xl font-semibold text-primary mb-2">
                  <a href={`/blog/${post.id}/`} class="hover:text-accent transition-colors">
                    {post.data.title}
                  </a>
                </h3>
                <p class="text-base text-text-secondary line-clamp-3 mb-4">
                  {post.data.description}
                </p>
                <Button variant="ghost" size="small" href={`/blog/${post.id}/`}>
                  Lire la suite
                </Button>
              </Card>
            ))}
          </div>
        </div>
      </section>
    </BaseLayout>
    ```

    Key points per locked decisions:
    - Uses Card, Badge, Button components (per D-05)
    - Each card has title H3 link, FormattedDate, description line-clamp-3, language Badge, ghost Button "Lire la suite" (per D-06)
    - Grid layout `grid-cols-1 md:grid-cols-2 gap-6` with `max-w-7xl mx-auto px-4 py-20` (per D-07)
    - Preserves BaseLayout with `headerTitle="Idees et Opinions"` (per D-08, note: headerTitle uses accent character as in current code)
  </action>
  <verify>
    <automated>cd /home/ng4e/gambetech/internal/repository/ng4e.github.io && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "import Card" src/pages/blog/index.astro
    - grep -q "import Badge" src/pages/blog/index.astro
    - grep -q "import Button" src/pages/blog/index.astro
    - grep -q "grid-cols-1 md:grid-cols-2 gap-6" src/pages/blog/index.astro
    - grep -q "line-clamp-3" src/pages/blog/index.astro
    - grep -q "Lire la suite" src/pages/blog/index.astro
    - grep -q "max-w-7xl mx-auto px-4" src/pages/blog/index.astro
    - npm run build completes without errors
  </acceptance_criteria>
  <done>Blog listing page uses Card/Badge/Button components with 2-column responsive grid, language badges, date display, description excerpts, and "Lire la suite" ghost CTA buttons. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance BlogPost prose styling and verify RSS feed</name>
  <files>src/layouts/BlogPost.astro</files>
  <read_first>src/layouts/BlogPost.astro, src/pages/rss.xml.js, src/pages/blog/[...slug].astro</read_first>
  <action>
    **1. Enhance prose styling in BlogPost.astro (per D-09, D-10):**

    The article tag currently has `class="prose prose-lg mx-auto max-w-3xl px-4 py-12"`. Add prose modifier classes for design system consistency:

    Update the article class to:
    ```
    prose prose-lg mx-auto max-w-3xl px-4 py-12 prose-headings:text-primary prose-a:text-accent prose-a:no-underline hover:prose-a:underline prose-blockquote:border-accent prose-strong:text-primary
    ```

    This ensures:
    - Headings use the marine/primary color
    - Links use the teal/accent color with underline on hover
    - Blockquote left border uses accent color
    - Bold text uses primary color

    Do NOT change the layout structure (per D-10) -- BlogPost.astro already correctly delegates to BaseLayout. Only add the prose modifier classes.

    Do NOT modify `[...slug].astro` (per D-11) -- it works correctly as-is.

    **2. Verify RSS feed (per D-15, D-16):**
    No code changes needed to `src/pages/rss.xml.js`. The RSS feed spreads `post.data` which includes title, description, pubDate. Adding `tags` to the schema causes tags to be spread into RSS items but `@astrojs/rss` ignores unknown fields -- this is safe.

    The verification step will confirm the RSS feed builds correctly by checking the build output includes `rss.xml`.
  </action>
  <verify>
    <automated>cd /home/ng4e/gambetech/internal/repository/ng4e.github.io && npm run build 2>&1 | tail -5 && test -f dist/rss.xml && echo "RSS OK" && cat dist/rss.xml | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "prose-headings:text-primary" src/layouts/BlogPost.astro
    - grep -q "prose-a:text-accent" src/layouts/BlogPost.astro
    - grep -q "prose-blockquote:border-accent" src/layouts/BlogPost.astro
    - npm run build completes without errors
    - dist/rss.xml exists and contains blog post titles
  </acceptance_criteria>
  <done>BlogPost.astro has prose modifier classes for design system colors. RSS feed builds successfully with all blog posts. Both blog post URLs render with prose typography. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build && npx astro check` passes with 0 errors
- Blog listing page at `/blog` uses Card, Badge, Button components in 2-column grid
- Blog posts at `/blog/001-building-for-passion-projects/` and `/blog/002-my-goto-stack-tools/` render with prose typography
- RSS feed at `/rss.xml` exists and contains both blog posts
- `grep -q "Card\|Badge\|Button" src/pages/blog/index.astro` confirms design system usage
</verification>

<success_criteria>
- Blog listing page restyled with design system components matching BlogPreviewSection pattern
- Blog post pages render markdown with design-system-consistent prose colors
- RSS feed functional with all blog posts after schema changes
- Full build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-migration/06-02-SUMMARY.md`
</output>
