---
phase: 06-content-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content.config.ts
  - src/content/blog/001-building-for-passion-projects.md
  - src/content/blog/002-my-goto-stack-tools.md
  - src/content/projects/acn_lbp.json
  - src/content/projects/acn_sg.json
autonomous: true
requirements:
  - CONT-01
  - CONT-02

must_haves:
  truths:
    - "Blog schema accepts tags field without build errors"
    - "Both blog posts have clean HTML with no Bulma classes"
    - "All 6 project JSON files validate against the Zod schema"
    - "heroImage references to missing files are removed from frontmatter"
  artifacts:
    - path: "src/content.config.ts"
      provides: "Blog schema with tags field"
      contains: "tags: z.array"
    - path: "src/content/blog/001-building-for-passion-projects.md"
      provides: "Cleaned blog post with no Bulma HTML"
    - path: "src/content/projects/acn_lbp.json"
      provides: "Project JSON with placeholder content"
      contains: "completer"
  key_links:
    - from: "src/content.config.ts"
      to: "src/content/blog/*.md"
      via: "Zod schema validation at build time"
      pattern: "tags.*z\\.array"
---

<objective>
Prepare all content for migration: update blog schema with tags field, clean legacy Bulma HTML from blog posts, fix broken heroImage references, and fill stub project JSON files with placeholder content.

Purpose: Content must validate cleanly against schemas and render correctly in prose context before the blog listing page can be restyled.
Output: Clean blog posts, updated schema, valid project JSONs.
</objective>

<execution_context>
@/home/ng4e/gambetech/internal/repository/ng4e.github.io/.claude/get-shit-done/workflows/execute-plan.md
@/home/ng4e/gambetech/internal/repository/ng4e.github.io/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-content-migration/06-CONTEXT.md
@.planning/phases/06-content-migration/06-RESEARCH.md

@src/content.config.ts
@src/content/blog/001-building-for-passion-projects.md
@src/content/blog/002-my-goto-stack-tools.md
@src/content/projects/acn_lbp.json
@src/content/projects/acn_sg.json

<interfaces>
<!-- Current blog schema from src/content.config.ts -->
```typescript
schema: z.object({
  title: z.string(),
  description: z.string(),
  pubDate: z.coerce.date(),
  updatedDate: z.coerce.date().optional(),
  heroImage: z.string().optional(),
  author: z.string().optional(),
  lang: z.enum(["fr", "en"]).default("en").optional(),
}),
```

<!-- Projects schema from src/content.config.ts -->
```typescript
schema: z.object({
  id: z.string(),
  key: z.number(),
  duration: z.string(),
  company: z.string(),
  client: z.string(),
  mission: z.string(),
  context: z.string(),
  role: z.string(),
  tasks: z.array(z.string()),
  technologies: z.array(z.string()),
  results: z.array(z.string()),
}),
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update blog schema and clean blog post content</name>
  <files>src/content.config.ts, src/content/blog/001-building-for-passion-projects.md, src/content/blog/002-my-goto-stack-tools.md</files>
  <read_first>src/content.config.ts, src/content/blog/001-building-for-passion-projects.md, src/content/blog/002-my-goto-stack-tools.md</read_first>
  <action>
    **1. Add tags field to blog schema (per D-02):**
    In `src/content.config.ts`, add `tags: z.array(z.string()).optional()` after the `lang` field in the blog schema object.

    **2. Clean blog post 001 legacy Bulma HTML (per D-03):**
    In `src/content/blog/001-building-for-passion-projects.md`, replace the two Bulma HTML blocks (lines 30-36 and lines 50-56) with prose-compatible HTML:

    Replace this pattern (appears twice):
    ```html
    <div class="has-text-centered">
      <a href="URL" target="_blank">
        <figure class="image is-128x128">
          <img src="/blog_img_xxx.yyy" alt="Alt text">
        </figure>
      </a>
    </div>
    ```

    With this prose-compatible pattern:
    ```html
    <div class="not-prose text-center my-6">
      <a href="URL" target="_blank">
        <img src="/blog_img_xxx.yyy" alt="Alt text" class="inline-block w-32 h-32 object-cover rounded" />
      </a>
    </div>
    ```

    First instance (Jeux Bamoun image): URL is `https://why.pirien.com/store/products/jeux-des-7-familles-edition-royaume-bamoun`, image is `/blog_img_sagam.jpg`, alt is `Jeux Bamoun`.
    Second instance (Talent Unearthed image): URL is `https://www.youtube.com/watch?v=3AFgEyw1fbE&t=86s`, image is `/blog_img_raguin.png`, alt is `Talent Unearthed Series`.

    **3. Remove broken heroImage references (per D-04):**
    Remove the `heroImage` line from frontmatter of BOTH blog posts. The referenced files (`/images/community-impact-hero.jpg`, `/images/stack-blog-hero.jpg`) do not exist and no template renders them.

    **4. Update author placeholder:**
    In both blog posts, change `author: "Your Name"` to `author: "Samuel Molu-N"` (derived from CV filename in codebase).
  </action>
  <verify>
    <automated>cd /home/ng4e/gambetech/internal/repository/ng4e.github.io && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "tags: z.array(z.string()).optional()" src/content.config.ts
    - grep -c "has-text-centered\|is-128x128" src/content/blog/001-building-for-passion-projects.md returns 0
    - grep -q "not-prose text-center" src/content/blog/001-building-for-passion-projects.md
    - grep -c "heroImage" src/content/blog/001-building-for-passion-projects.md returns 0
    - grep -c "heroImage" src/content/blog/002-my-goto-stack-tools.md returns 0
    - grep -q 'author: "Samuel Molu-N"' src/content/blog/001-building-for-passion-projects.md
    - npm run build completes without errors
  </acceptance_criteria>
  <done>Blog schema includes tags field. Both blog posts have no Bulma HTML, no broken heroImage refs, and real author name. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fill stub project JSON files with placeholder content</name>
  <files>src/content/projects/acn_lbp.json, src/content/projects/acn_sg.json</files>
  <read_first>src/content/projects/acn_lbp.json, src/content/projects/acn_sg.json, src/content/projects/slashup.json</read_first>
  <action>
    **Fill both stub project JSONs with placeholder content (per D-13):**

    Preserve existing real metadata (id, key, duration, company, client) and fill empty fields with placeholder text so Phase 7 (/experiences page) can display them.

    For `acn_lbp.json` (Accenture / La Banque Postale):
    ```json
    {
      "id": "accenture-lbp",
      "key": 4,
      "duration": "Octobre 2018 - Mars 2020",
      "company": "Accenture",
      "client": "La Banque Postale",
      "mission": "À compléter",
      "context": "À compléter",
      "role": "À compléter",
      "tasks": ["À compléter"],
      "technologies": ["À compléter"],
      "results": ["À compléter"]
    }
    ```

    For `acn_sg.json` (Accenture / Societe Generale):
    ```json
    {
      "id": "accenture-sg",
      "key": 5,
      "duration": "Octobre 2018 - Mars 2020",
      "company": "Accenture",
      "client": "Société Générale",
      "mission": "À compléter",
      "context": "À compléter",
      "role": "À compléter",
      "tasks": ["À compléter"],
      "technologies": ["À compléter"],
      "results": ["À compléter"]
    }
    ```

    No changes to the projects content schema (per D-14).
  </action>
  <verify>
    <automated>cd /home/ng4e/gambetech/internal/repository/ng4e.github.io && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "compléter" src/content/projects/acn_lbp.json
    - grep -q "compléter" src/content/projects/acn_sg.json
    - Both files have non-empty mission, context, role fields
    - npm run build completes without errors
  </acceptance_criteria>
  <done>All 6 project JSON files have non-empty content and validate against the Zod schema. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no content collection errors
- `npx astro check` passes with 0 errors
- No Bulma CSS classes remain in any blog post: `grep -r "has-text-centered\|is-128x128" src/content/blog/`
- Blog schema includes tags field: `grep "tags" src/content.config.ts`
- All project JSONs have content: `grep "compléter" src/content/projects/acn_*.json`
</verification>

<success_criteria>
- Build passes with updated schema and cleaned content
- Zero Bulma HTML classes in blog posts
- All 6 project JSON files have non-empty required fields
- heroImage references removed from frontmatter (files don't exist)
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-migration/06-01-SUMMARY.md`
</output>
